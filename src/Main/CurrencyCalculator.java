package Main;

import GUI.GUI;

public class CurrencyCalculator {
    public static void main(String[] args) {
        System.out.println("\nStarting...  " + GUI.title + " (" + GUI.version + ")\n");
        GUI.drawGUI();
    }
}